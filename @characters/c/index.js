const c = 'c';

// Lowercase 'c'
c.lowercase = 'c';

// Uppercase 'C'
c.uppercase = 'C';

// Unicode variations
// Different language/script variations of 'c'
c.unicode = {
  greek: 'γ',     // Lowercase greek variant
  cyrillic: 'ц',  // Cyrillic variant
};

// Phonetic representation
c.phonetic = 'see';

// Morse code representation
c.morse = '-.-.';

// ASCII art representation
c.asciiArt = ` .---.
|     \
|     /
 '---'
`;

export default c;
