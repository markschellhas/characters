const t = 't';

// Lowercase 't'
t.lowercase = 't';

// Uppercase 'T'
t.uppercase = 'T';

// Unicode variations
// Different language/script variations of 't'
t.unicode = {
  greek: 'τ',     // Lowercase greek variant
  cyrillic: 'т',  // Cyrillic variant
};

// Phonetic representation
t.phonetic = 'tee';

// Morse code representation
t.morse = '-';

// ASCII art representation
t.asciiArt = `.---.
  |
  |
  |
`;

export default t;
