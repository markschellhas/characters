const n = 'n';

// Lowercase 'n'
n.lowercase = 'n';

// Uppercase 'N'
n.uppercase = 'N';

// Unicode variations
// Different language/script variations of 'n'
n.unicode = {
  greek: 'ν',     // Lowercase greek variant
  cyrillic: 'н',  // Cyrillic variant
};

// Phonetic representation
n.phonetic = 'en';

// Morse code representation
n.morse = '-.';

// ASCII art representation
n.asciiArt = `|\   |
| \  |
|  \ |
`;

export default n;
