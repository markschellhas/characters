const u = 'u';

// Lowercase 'u'
u.lowercase = 'u';

// Uppercase 'U'
u.uppercase = 'U';

// Unicode variations
// Different language/script variations of 'u'
u.unicode = {
  greek: 'υ',     // Lowercase greek variant
  cyrillic: 'у',  // Cyrillic variant
};

// Phonetic representation
u.phonetic = 'you';

// Morse code representation
u.morse = '..-';

// ASCII art representation
u.asciiArt = `|     |
|     |
 '---'
`;

export default u;
