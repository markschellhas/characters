const k = 'k';

// Lowercase 'k'
k.lowercase = 'k';

// Uppercase 'K'
k.uppercase = 'K';

// Unicode variations
// Different language/script variations of 'k'
k.unicode = {
  greek: 'κ',     // Lowercase greek variant
  cyrillic: 'к',  // Cyrillic variant
};

// Phonetic representation
k.phonetic = 'kay';

// Morse code representation
k.morse = '-.-';

// ASCII art representation
k.asciiArt = `|    /
|   /
|__/
`;

export default k;
