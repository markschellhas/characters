const p = 'p';

// Lowercase 'p'
p.lowercase = 'p';

// Uppercase 'P'
p.uppercase = 'P';

// Unicode variations
// Different language/script variations of 'p'
p.unicode = {
  greek: 'π',     // Lowercase greek variant
  cyrillic: 'п',  // Cyrillic variant
};

// Phonetic representation
p.phonetic = 'pee';

// Morse code representation
p.morse = '.--.';

// ASCII art representation
p.asciiArt = ` .---.
|     \
 '---'
`;

export default p;
