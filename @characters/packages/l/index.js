const l = 'l';

// Lowercase 'l'
l.lowercase = 'l';

// Uppercase 'L'
l.uppercase = 'L';

// Unicode variations
// Different language/script variations of 'l'
l.unicode = {
  greek: 'λ',     // Lowercase greek variant
  cyrillic: 'л',  // Cyrillic variant
};

// Phonetic representation
l.phonetic = 'el';

// Morse code representation
l.morse = '.-..';

// ASCII art representation
l.asciiArt = `|     
|     
|_____
`;

export default l;
