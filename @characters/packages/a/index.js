const a = 'a';

// Lowercase 'a'
a.lowercase = 'a';

// Uppercase 'A'
a.uppercase = 'A';

// Unicode variations
// Different language/script variations of 'a'
a.unicode = {
  greek: 'α',     // Lowercase greek variant
  cyrillic: 'а',  // Cyrillic variant
};

// Phonetic representation
a.phonetic = 'ay';

// Morse code representation
a.morse = '.-';

// ASCII art representation
a.asciiArt = `  .---.
 /     \
|       |
 '---'
`;

export default a;
