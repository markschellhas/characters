const g = 'g';

// Lowercase 'g'
g.lowercase = 'g';

// Uppercase 'G'
g.uppercase = 'G';

// Unicode variations
// Different language/script variations of 'g'
g.unicode = {
  greek: 'γ',     // Lowercase greek variant
  cyrillic: 'г',  // Cyrillic variant
};

// Phonetic representation
g.phonetic = 'jee';

// Morse code representation
g.morse = '--.';

// ASCII art representation
g.asciiArt = ` .---.
|     \
|     G
 '---'
`;

export default g;
