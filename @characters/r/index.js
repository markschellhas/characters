const r = 'r';

// Lowercase 'r'
r.lowercase = 'r';

// Uppercase 'R'
r.uppercase = 'R';

// Unicode variations
// Different language/script variations of 'r'
r.unicode = {
  greek: 'ρ',     // Lowercase greek variant
  cyrillic: 'р',  // Cyrillic variant
};

// Phonetic representation
r.phonetic = 'are';

// Morse code representation
r.morse = '.-.';

// ASCII art representation
r.asciiArt = ` .---.
|    /
 '---'
`;

export default r;
