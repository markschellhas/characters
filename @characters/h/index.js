const h = 'h';

// Lowercase 'h'
h.lowercase = 'h';

// Uppercase 'H'
h.uppercase = 'H';

// Unicode variations
// Different language/script variations of 'h'
h.unicode = {
  greek: 'η',     // Lowercase greek variant
  cyrillic: 'н',  // Cyrillic variant
};

// Phonetic representation
h.phonetic = 'aitch';

// Morse code representation
h.morse = '....';

// ASCII art representation
h.asciiArt = `|     |
|     |
|_____|
|     |
`;

export default h;
