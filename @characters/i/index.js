const i = 'i';

// Lowercase 'i'
i.lowercase = 'i';

// Uppercase 'I'
i.uppercase = 'I';

// Unicode variations
// Different language/script variations of 'i'
i.unicode = {
  greek: 'ι',     // Lowercase greek variant
  cyrillic: 'и',  // Cyrillic variant
};

// Phonetic representation
i.phonetic = 'eye';

// Morse code representation
i.morse = '..';

// ASCII art representation
i.asciiArt = `|
|
|
|
`;

export default i;
