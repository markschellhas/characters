const y = 'y';

// Lowercase 'y'
y.lowercase = 'y';

// Uppercase 'Y'
y.uppercase = 'Y';

// Unicode variations
// Different language/script variations of 'y'
y.unicode = {
  greek: 'ψ',     // Lowercase greek variant
  cyrillic: 'у',  // Cyrillic variant
};

// Phonetic representation
y.phonetic = 'why';

// Morse code representation
y.morse = '-.--';

// ASCII art representation
y.asciiArt = `|   |
 \ /
  |
`;

export default y;
