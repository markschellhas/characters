const b = 'b';

// Lowercase 'b'
b.lowercase = 'b';

// Uppercase 'B'
b.uppercase = 'B';

// Unicode variations
// Different language/script variations of 'b'
b.unicode = {
  greek: 'β',     // Lowercase greek variant
  cyrillic: 'б',  // Cyrillic variant
};

// Phonetic representation
b.phonetic = 'bee';

// Morse code representation
b.morse = '-...';

// ASCII art representation
b.asciiArt = ` ----
|    \
|    /
 ----
`;

export default b;
