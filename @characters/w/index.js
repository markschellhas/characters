const w = 'w';

// Lowercase 'w'
w.lowercase = 'w';

// Uppercase 'W'
w.uppercase = 'W';

// Unicode variations
// Different language/script variations of 'w'
w.unicode = {
  greek: 'ω',     // Lowercase greek variant
  cyrillic: 'ш',  // Cyrillic variant
};

// Phonetic representation
w.phonetic = 'double-you';

// Morse code representation
w.morse = '.--';

// ASCII art representation
w.asciiArt = `|  |  |
 \/  \/
`;

export default w;
