const s = 's';

// Lowercase 's'
s.lowercase = 's';

// Uppercase 'S'
s.uppercase = 'S';

// Unicode variations
// Different language/script variations of 's'
s.unicode = {
  greek: 'σ',     // Lowercase greek variant
  cyrillic: 'с',  // Cyrillic variant
};

// Phonetic representation
s.phonetic = 'ess';

// Morse code representation
s.morse = '...';

// ASCII art representation
s.asciiArt = `  .--.
 /    \
|    |
 '--'
`;

export default s;
