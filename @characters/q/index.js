const q = 'q';

// Lowercase 'q'
q.lowercase = 'q';

// Uppercase 'Q'
q.uppercase = 'Q';

// Unicode variations
// Different language/script variations of 'q'
q.unicode = {
  greek: 'θ',     // Lowercase greek variant
  cyrillic: 'к',  // Cyrillic variant
};

// Phonetic representation
q.phonetic = 'queue';

// Morse code representation
q.morse = '--.-';

// ASCII art representation
q.asciiArt = ` .---.
|     Q
 '---'
`;

export default q;
