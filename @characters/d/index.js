const d = 'd';

// Lowercase 'd'
d.lowercase = 'd';

// Uppercase 'D'
d.uppercase = 'D';

// Unicode variations
// Different language/script variations of 'd'
d.unicode = {
  greek: 'δ',     // Lowercase greek variant
  cyrillic: 'д',  // Cyrillic variant
};

// Phonetic representation
d.phonetic = 'dee';

// Morse code representation
d.morse = '-..';

// ASCII art representation
d.asciiArt = ` ----
|    \
|    |
 ----
`;

export default d;
