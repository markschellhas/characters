const m = 'm';

// Lowercase 'm'
m.lowercase = 'm';

// Uppercase 'M'
m.uppercase = 'M';

// Unicode variations
// Different language/script variations of 'm'
m.unicode = {
  greek: 'μ',     // Lowercase greek variant
  cyrillic: 'м',  // Cyrillic variant
};

// Phonetic representation
m.phonetic = 'em';

// Morse code representation
m.morse = '--';

// ASCII art representation
m.asciiArt = `/\/\
|  |
|  |
`;

export default m;
