const v = 'v';

// Lowercase 'v'
v.lowercase = 'v';

// Uppercase 'V'
v.uppercase = 'V';

// Unicode variations
// Different language/script variations of 'v'
v.unicode = {
  greek: 'ν',     // Lowercase greek variant
  cyrillic: 'в',  // Cyrillic variant
};

// Phonetic representation
v.phonetic = 'vee';

// Morse code representation
v.morse = '...-';

// ASCII art representation
v.asciiArt = `|     |
 \   /
  \ /
`;

export default v;
